import React from 'react';

const Form = () => (
  <form>
    <label htmlFor="email">Email</label>
    <input id="email" />
    <label htmlFor="password">Password</label>
    <input id="password" />
    <input type="submit" />
  </form>
);

export default Form;
